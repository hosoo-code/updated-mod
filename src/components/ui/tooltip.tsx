"use client";
import { useId, type ReactNode } from "react";
export function Tooltip({ content, children, side = "top" }: { content: ReactNode; children: ReactNode; side?: "top" | "bottom"; }) { const id = useId(); return <span className="group relative inline-flex" aria-describedby={id}>{children}<span id={id} role="tooltip" className={["pointer-events-none absolute left-1/2 z-50 w-max max-w-[240px] -translate-x-1/2 rounded-xl border border-zinc-800 bg-zinc-950 px-3 py-1.5 text-center text-xs font-medium leading-5 text-white opacity-0 shadow-lg transition-all duration-150 group-hover:opacity-100 group-focus-within:opacity-100 dark:border-white/10 dark:bg-white dark:text-zinc-950", side === "top" ? "bottom-full mb-2 translate-y-1 group-hover:translate-y-0 group-focus-within:translate-y-0" : "top-full mt-2 -translate-y-1 group-hover:translate-y-0 group-focus-within:translate-y-0"].join(" ")}>{content}</span></span>; }
